-- Search_spec.lua
-- Sandbox tests for search text processing

describe("Search.NormalizeText", function()
    it("lowercases input", function()
        pending("Mechanic sandbox setup required")
    end)
    
    it("strips color codes", function()
        pending("Mechanic sandbox setup required")
    end)
    
    it("handles nil input", function()
        pending("Mechanic sandbox setup required")
    end)
end)

describe("Search.MatchesFilter", function()
    it("matches substrings", function()
        pending("Mechanic sandbox setup required")
    end)
    
    it("is case-insensitive", function()
        pending("Mechanic sandbox setup required")
    end)
    
    it("handles empty filter", function()
        pending("Mechanic sandbox setup required")
    end)
end)
